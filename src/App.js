import React from "react";
import Components from "./components";

const App = (props) => {
    // console.log(props.children)
    return (
        <div>
            <Components />
        </div>
    ); 
}
export default App;